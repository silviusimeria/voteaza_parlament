<div class="overflow-hidden">
  <div class="inset-y-0 right-0 w-full max-w-2xl bg-white shadow-xl flex flex-col">
    <div class="flex justify-between items-center px-6 py-4 border-b border-gray-200 bg-white">
      <h2 class="text-xl font-bold text-gray-900">
        <%= county.name %>
      </h2>
    </div>

    <div class="flex-1 overflow-y-auto bg-gray-50">
      <%= render partial: 'parties_list', locals: { county: county } %>
    </div>
  </div>
</div>